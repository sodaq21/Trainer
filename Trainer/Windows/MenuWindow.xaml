<Window x:Class="Trainer.Windows.MenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Trainer.Windows"
        mc:Ignorable="d"
        Title="Trainer - Menu" Height="360" Width="450" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Background="{DynamicResource WindowBg}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Style="{StaticResource TitleText}" Text="Choose the mode" Margin="0 35 0 0" FontSize="34"/>
        <Button Grid.Row="1" Grid.ColumnSpan="2" Content="Aim Trainer" Style="{StaticResource IconButton}" 
                Click="AimBtn_Click" Width="300" Height="85">
            <Button.Tag>
                <Image x:Name="menu_crosshair" Source="{DynamicResource CrosshairIcon}" Width="50" Height="50"/>
            </Button.Tag>
        </Button>

        <Button Grid.Row="2" Grid.ColumnSpan="2" Content="Reaction Time" Style="{StaticResource IconButton}" 
                Click="ReactionBtn_Click" Width="300" Height="85" Margin="0 0 0 20">
            <Button.Tag>
                <Image Source="{DynamicResource TimerIcon}"/>
            </Button.Tag>
        </Button>

        <Button Grid.Row="0" Grid.Column="1" Style="{StaticResource ServiceButton}" Click="SwitchTheme">
            <Image Source="{DynamicResource SwitchIcon}"/>
        </Button>
    </Grid>
</Window>
