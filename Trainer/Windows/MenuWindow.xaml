<Window x:Class="Trainer.Windows.MenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Trainer - Menu" Height="360" Width="450" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock DockPanel.Dock="Left" Style="{StaticResource HeaderText}" Text="Choose mode"/>
            <Button DockPanel.Dock="Right" Style="{StaticResource ServiceBtn}" Click="ThemeToggle_Click"
                    ToolTip="Switch theme" Content="{DynamicResource ThemeToggleGlyph}" FontSize="18"/>
        </DockPanel>

        <Border Grid.Row="1" Style="{StaticResource SurfacePanel}">
            <StackPanel VerticalAlignment="Center">
                <Button Style="{StaticResource MenuBtn}" Content="Aim Trainer" Click="AimBtn_Click"/>
                <Button Style="{StaticResource MenuBtn}" Content="Reaction Time" Click="ReactionBtn_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
